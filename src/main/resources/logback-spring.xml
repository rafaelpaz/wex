<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.wex.purchasetransactions" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.flywaydb" level="INFO"/>

    <springProfile name="docker">
        <logger name="com.wex.purchasetransactions" level="INFO"/>
        <logger name="org.hibernate.SQL" level="${HIBERNATE_SQL_LOG_LEVEL:-WARN}"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="${HIBERNATE_BIND_LOG_LEVEL:-WARN}"/>
        <logger name="org.springframework.web" level="WARN"/>
    </springProfile>

    <springProfile name="local">
        <logger name="com.wex.purchasetransactions" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="INFO"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="INFO"/>
    </springProfile>

</configuration>
